<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Pay Utility Bill"
        android:textColor="@color/apple_text_primary"
        android:textSize="20sp"
        android:fontFamily="sans-serif-black"
        android:layout_marginBottom="24dp"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Select Bill Category"
        android:textColor="@color/apple_text_secondary"
        android:textSize="12sp"
        android:textAllCaps="true"
        android:fontFamily="sans-serif-bold"
        android:layout_marginBottom="8dp"/>

    <Spinner
        android:id="@+id/spinner_bill_type"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:background="@drawable/edittext_border"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:layout_marginBottom="16dp"/>

    <com.google.android.material.textfield.TextInputLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Amount (₹)"
        style="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox"
        app:boxStrokeColor="@color/apple_blue"
        app:hintTextColor="@color/apple_blue"
        android:layout_marginBottom="24dp">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/editText_bill_amount"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="number"
            android:fontFamily="sans-serif-medium"/>
    </com.google.android.material.textfield.TextInputLayout>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Payment Source"
        android:textColor="@color/apple_text_secondary"
        android:textSize="12sp"
        android:textAllCaps="true"
        android:fontFamily="sans-serif-bold"
        android:layout_marginBottom="8dp"/>

    <Spinner
        android:id="@+id/spinner_source_account"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:background="@drawable/edittext_border"
        android:paddingStart="12dp"
        android:paddingEnd="12dp" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="* Funds will be deducted instantly from the selected account."
        android:textColor="@color/apple_text_secondary"
        android:textSize="11sp"
        android:fontFamily="sans-serif-italic"/>

</LinearLayout>
